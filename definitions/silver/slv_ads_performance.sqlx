config { 
  type: "table", 
  schema: data.silver_schema,
  name: data.tab_slv_ads
}
SELECT
  _DATA_DATE AS data,
  CampaignName AS campanha,
  Cost / 1000000 AS custo,
  Clicks AS cliques
FROM `${data.project_id}.${data.raw_ads}.${data.raw_ads_table}`